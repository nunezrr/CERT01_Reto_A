{
  "name": "qa-dummy-app-compat",
  "version": "1.0.0",
  "private": true,
  "description": "Compatibilidad temporal: reenvia scripts a backend",
  "scripts": {
    "start-server": "node ../backend/server.js",
    "start-worker": "node ../backend/worker.js"
  }
}
