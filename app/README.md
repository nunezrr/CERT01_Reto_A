# App (Compatibilidad)

Esta carpeta existe para mantener comandos antiguos durante la migracion.

Comandos disponibles:

- npm run start-server
- npm run start-worker

Ambos ejecutan el codigo real ubicado en ../backend.

Estructura recomendada:

- backend/ para API y worker
- frontend/ para UI
- tests/ para Playwright
